# Development guide

## Available commands

```sh
# Run tests
bun run test:run

# Lint and typecheck
# Never run `tsc` for typechecking, `bun lint` does it already
bun lint

# Format code
bun format
```
